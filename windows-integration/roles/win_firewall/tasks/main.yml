---
- name: Create firewall rule for service
  win_firewall_rule:
    name: '{{ service_name }}'
    localport: '{{ local_port }}'
    action: allow
    direction: in
    protocol: '{{ protocol_name }}'
    state: present
    enabled: yes